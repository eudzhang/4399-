import type {PlayerState,Rect} from './types';const GRAVITY=1500,MOVE=230,JUMP=570,MAX_FALL=850;export const overlaps=(a:Rect,b:Rect)=>a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;
export function movePlayer(p:PlayerState,solids:Rect[],dt:number){p.vx=(Number(p.input.right)-Number(p.input.left))*MOVE;if(p.input.jump&&p.onGround){p.vy=-JUMP;p.onGround=false}p.vy=Math.min(MAX_FALL,p.vy+GRAVITY*dt);p.x+=p.vx*dt;for(const s of solids)if(overlaps(p,s)){if(p.vx>0)p.x=s.x-p.w;else if(p.vx<0)p.x=s.x+s.w;p.vx=0}p.y+=p.vy*dt;p.onGround=false;for(const s of solids)if(overlaps(p,s)){if(p.vy>0){p.y=s.y-p.h;p.onGround=true}else if(p.vy<0)p.y=s.y+s.h;p.vy=0}p.x=Math.max(0,Math.min(960-p.w,p.x))}
